---
layout: review_post
title: "[Paper Review] Large Language Model for Table Processing: A Survey"
author: Sohyun
date: 2024-08-19
---

### [Archived Survey]

https://arxiv.org/abs/2402.05121


테이블은 이차원 구조로 많은 데이터를 저장하며, 데이터베이스 질의, 스프레드시트 조작, 웹 테이블 질문 응답, 이미지 테이블 정보 추출 등 다양한 일상 작업에서 중요한 역할을 한다. 대형 언어 모델(LLMs)과 시각 언어 모델(VLMs)을 사용해 이러한 테이블 작업을 자동화하면 당연하게도 여러 분야에 큰 이익을 제공하며, 학계와 산업계의 주목을 받고 있다고 한다(잘 모른다). 이 서베이 페이퍼는 테이블 관련 작업과 그에 따른 사용자 시나리오 / 기술적 측면을 포괄적으로 검토하며, 전통적인 작업인 테이블 QA 뿐만 아니라 스프레드시트 조작(증강), 테이블 데이터 분석 등 새로운 분야도 다룬다. 또한, LLM과 VLM을 테이블 처리에 맞게 훈련시키는 기법과 LLM 기반 에이전트를 사용하는 프롬프트 엔지니어링을 논의하고, 사용자 의도 파악 및 다양한 테이블 출처에서 정보 추출과 같은 주요 도전 과제를 강조한다.


서베이 페이퍼인 만큼 위에서 아래로 순서대로 차근차근 살펴보려 한다.



## 1 Introduction



현대의 데이터 중심 시대에서는 테이블 형식으로 구조화된 대량의 데이터가 저장되며, 이러한 데이터를 처리하는 작업(예: 데이터베이스 질의, 스프레드시트 조작, 웹 테이블 QA, 이미지 테이블 정보 추출)이 일상적으로 발생한다. 이러한 작업들은 반복적이거나 오류 발생이 쉬우며, 때로는 전문적인 기술을 필요로 하므로 간소화될 필요가 있다.

  

최근 LLMs는 다양한 작업에서 그 효과와 다재다능함을 입증하며, 이를 테이블 관련 작업에 적용하려는 연구가 활발해졌다. 그러나 테이블의 구조는 LLM이 사전 훈련된 텍스트 데이터와는 달리, 고유한 특성을 가지고 있어 LLM이 이해하기 어렵다.

  

테이블 작업의 주요 도전 과제는 다음과 같다:

  

•  **구조화된 데이터:** 테이블은 행과 열로 구성된 구조화된 데이터로, LLM이 다차원적 측면을 이해하는 데 어려움을 겪는다.

•  **복잡한 추론:** 테이블 작업은 복잡한 추론과 다단계 문제 해결을 요구한다.

•  **외부 도구 활용:** 테이블 작업에서 Microsoft Excel, Python, SQL 같은 외부 도구를 사용해야 하며, LLM이 이러한 도구들과 통합되어야 한다.

  

이러한 과제를 해결하기 위해, LLM을 테이블 작업에 맞게 조정하는 방법이 필요하다. 초기 연구들은 TaBERT, TaPas, TURL, TaPEx 같은 모델을 사전 훈련하거나 미세 조정하는 방법을 사용했지만, 최신 접근법은 LLM의 강력한 추론 능력을 활용한 에이전트나 프롬프트 방식을 채택하고 있다. 이러한 방법들은 Instruction tuning, In Context Learning, Chain-of-thought 추론, Autonomous agents와 같은 LLM-specific 기술들을 활용하여 테이블 작업을 보다 포괄적으로 다루고 있다.
 


## 2 Table Types and Table Tasks



### 2.1 Table Definition


테이블은 거의 모든 도메인에서 지식과 정보를 조직하고 조작하는 데 널리 사용되는 데이터 구조이다. 이 문단에서는 테이블 형식, 테이블 작업, 그리고 테이블 데이터의 수명 주기에 대해 간략히 요약하고 있다.


이 논문은 주로 (반)구조화된 테이블에 초점을 맞추고 있으며, 이러한 테이블은 행과 열로 구성된 그리드 형태로, 각 열은 특정 속성을 나타내고 각 행은 다양한 속성 값을 포함한 레코드로 구성된다. 테이블의 주요 형식은 다음과 같다:

  

1. **스프레드시트 (SS)**: Google Sheets와 Microsoft Excel이 대표적이며, 사용자가 데이터를 쉽게 표시하고 조작할 수 있도록 다양한 기능을 제공한다. 병합된 셀, 계층적 열 등 불규칙한 레이아웃이 특징이며, 교사, 인사 부서, 영업 사원 등이 주로 사용한다.

2. **웹 테이블 (WT)**: HTML, 마크다운, JSON, XML 등의 형식으로 존재하며, 웹 페이지에 포함된 테이블로, 페이지 제목이나 주변 텍스트와 같은 맥락 정보가 함께 제공된다. 위키백과의 테이블이 대표적인 예로, 이를 추출하여 질문 응답 등에 활용할 수 있다.

3. **데이터베이스 (DB)**: 관계형 데이터베이스의 테이블로, 명확하게 정의된 스키마를 가지고 있으며, OLTP와 OLAP 시스템으로 구분된다. 데이터베이스는 확장 가능하고 안정적이며, 데이터 정확성과 무결성이 요구되는 기업에서 사용된다.

4. **문서 내 테이블 (DOC)**: 이미지, PDF, Word 파일 등에 포함된 테이블로, 구매 주문서, 재무 보고서, 영수증 등의 문서에 사용된다. 이러한 테이블은 텍스트로 둘러싸여 있어 추출 및 구조화가 필요하며, 불일치된 레이아웃이 정보 이해에 장애가 될 수 있다.

  

이 네 가지 테이블 형식은 각기 다른 사용자 시나리오에 맞추어져 있으며, AI 모델을 통해 다양한 문제를 해결할 수 있다. 예를 들어, 스프레드시트 시스템은 사용자 조작을 자동화하고, 웹 테이블은 질문 응답에 활용되며, 데이터베이스는 NL2SQL 기술을 통합해 데이터 엔지니어링을 지원한다. 문서 내 테이블은 식별, 구조화, 변환이 필요하다.



<!--stackedit_data:
eyJoaXN0b3J5IjpbLTcxNTI3NTIzNCwtNzI1ODkwMzg0LC0xNz
cwODE1NTUwLDE3OTE3OTY4OTcsMTg5NDEyODU5OSwxNzYxNjE3
MTY1LC00OTMyODU5MDUsLTE4Mzc0NzY4OTQsMTkxODI0Mjg4OC
wtMTAyOTk3MTc3MiwxMDI3NzMxMjE3LDEyMjAzMTc5MDUsLTc3
NzY3MTA4Myw2NDkxMjU0MTcsODMzNzk4MDA0XX0=
-->